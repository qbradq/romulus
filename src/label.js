function Label(name, value, length, mode, synonym) {
    this.name = name;
    this.value = value;
    this.length = length;
    this.mode = mode;
    this.synonym = synonym;
}

module.exports = Label;
